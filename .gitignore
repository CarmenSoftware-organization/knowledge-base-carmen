# Windows reserved names (Git cannot index these)
nul
con
prn
aux

# Migration temp (optional: remove if you want to commit the zip)
docscarmencloud.zip

# Backend local git clone (created by Wiki sync; don't commit)
backend/main/

# Wiki import staging (output of scripts/wiki-import-from-carmencloud.js)
wiki-import/

# Environment variables
.env
.env.local
.env.*.local
**/.env
**/.env.local
**/.env.*.local

# Wiki.js dry-run / local API token (use scripts/wikijs-dry-run-compare.sh)
backend/.wikijs.token
backend/wikijs.token

# Docker Compose — local secrets (use docker-compose.env.example as template)
.env.docker
.env.docker.local
docker-compose.override.yml

# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
build/
out/
.next/
.svelte-kit/

# Vite
.vite/

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
bash.exe.stackdump
origin_main_files.txt

# OS files
.DS_Store
Thumbs.db
